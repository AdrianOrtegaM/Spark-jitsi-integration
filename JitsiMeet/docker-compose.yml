services:
  web:
    image: jitsi/web:stable
    restart: unless-stopped
    ports:
      - "${HTTP_PORT}:80"
      - "${HTTPS_PORT}:443"
    volumes:
      - "${CONFIG}/web:/config:Z"
      - "${CONFIG}/web/letsencrypt:/etc/letsencrypt:Z"
    environment:
      - TZ=${TZ}
      - PUBLIC_URL=${PUBLIC_URL}

      - ENABLE_AUTH=${ENABLE_AUTH}
      - ENABLE_GUESTS=${ENABLE_GUESTS}
      - ENABLE_P2P=${ENABLE_P2P}

      - XMPP_DOMAIN=${XMPP_DOMAIN}
      - XMPP_AUTH_DOMAIN=${XMPP_AUTH_DOMAIN}
      - XMPP_MUC_DOMAIN=${XMPP_MUC_DOMAIN}
      - XMPP_GUEST_DOMAIN=${XMPP_GUEST_DOMAIN}

      - XMPP_BOSH_URL_BASE=http://prosody:5280
      - XMPP_WEB_SOCKET_DOMAIN=${XMPP_DOMAIN}
      - XMPP_WEB_SOCKET_URL=wss://${XMPP_DOMAIN}/xmpp-websocket
    depends_on:
      - prosody

  prosody:
    image: jitsi/prosody:stable
    restart: unless-stopped
    volumes:
      - "${CONFIG}/prosody:/config:Z"
    environment:
      - TZ=${TZ}

      - ENABLE_AUTH=${ENABLE_AUTH}
      - ENABLE_GUESTS=${ENABLE_GUESTS}

      - XMPP_DOMAIN=${XMPP_DOMAIN}
      - XMPP_AUTH_DOMAIN=${XMPP_AUTH_DOMAIN}
      - XMPP_MUC_DOMAIN=${XMPP_MUC_DOMAIN}
      - XMPP_INTERNAL_MUC_DOMAIN=${XMPP_INTERNAL_MUC_DOMAIN}
      - XMPP_GUEST_DOMAIN=${XMPP_GUEST_DOMAIN}

      - JICOFO_AUTH_USER=${JICOFO_AUTH_USER}
      - JICOFO_AUTH_PASSWORD=${JICOFO_AUTH_PASSWORD}

      - JVB_AUTH_USER=${JVB_AUTH_USER}
      - JVB_AUTH_PASSWORD=${JVB_AUTH_PASSWORD}

  jicofo:
    image: jitsi/jicofo:stable
    restart: unless-stopped
    volumes:
      - "${CONFIG}/jicofo:/config:Z"
    environment:
      - TZ=${TZ}
      - XMPP_SERVER=prosody
      - XMPP_PORT=5222

      - XMPP_DOMAIN=${XMPP_DOMAIN}
      - XMPP_AUTH_DOMAIN=${XMPP_AUTH_DOMAIN}
      - XMPP_MUC_DOMAIN=${XMPP_MUC_DOMAIN}
      - XMPP_INTERNAL_MUC_DOMAIN=${XMPP_INTERNAL_MUC_DOMAIN}

      - JICOFO_AUTH_USER=${JICOFO_AUTH_USER}
      - JICOFO_AUTH_PASSWORD=${JICOFO_AUTH_PASSWORD}
    depends_on:
      - prosody

  jvb:
    image: jitsi/jvb:stable
    restart: unless-stopped
    #NO host network: así SÍ resuelve "prosody"
    ports:
      - "10000:10000/udp"
    volumes:
      - "${CONFIG}/jvb:/config:Z"
    environment:
      - TZ=${TZ}
      - XMPP_SERVER=prosody
      - XMPP_PORT=5222

      - XMPP_AUTH_DOMAIN=${XMPP_AUTH_DOMAIN}
      - XMPP_INTERNAL_MUC_DOMAIN=${XMPP_INTERNAL_MUC_DOMAIN}

      - JVB_AUTH_USER=${JVB_AUTH_USER}
      - JVB_AUTH_PASSWORD=${JVB_AUTH_PASSWORD}

      - JVB_ADVERTISE_IPS=${JVB_ADVERTISE_IPS}
      - JVB_UDP_HARVESTER_DISABLED=${JVB_UDP_HARVESTER_DISABLED}
      - JVB_TCP_HARVESTER_DISABLED=${JVB_TCP_HARVESTER_DISABLED}
      - JVB_STUN_SERVERS=${JVB_STUN_SERVERS}
    depends_on:
      - prosody
